import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0000FF",
    },
    secondary: {
      main: "#F8F8FF",
    },
  },
});

export default theme;